source 'https://rubygems.org'
ruby '2.2.0'
gem 'rails', '4.2.0'
gem 'sass-rails', '~> 4.0.1'
gem 'uglifier', '>= 1.3.0'
gem 'coffee-rails', '~> 4.0.0'
gem 'jquery-rails'
gem 'turbolinks'
gem 'jbuilder', '~> 2.0'
gem 'spring', group: :development
gem 'foundation-rails'
gem 'gibbon'
gem 'google_drive'
gem 'high_voltage'
gem 'simple_form'
group :development do
  gem 'better_errors'
  gem 'quiet_assets'
  gem 'rails_layout'
  gem 'sqlite3'
end
group :production do
  gem 'pg'
  gem 'rails_12factor'
  gem 'thin'
end

# new app defaults

# Use sqlite3 as the database for Active Record
# Use SCSS for stylesheets: sass-rails
# Use Uglifier as compressor for JavaScript assets
# Use CoffeeScript for .js.coffee assets and views: coffee-rails
# See https://github.com/sstephenson/execjs#readme for more supported runtimes
# gem 'therubyracer',  platforms: :ruby

# Use jquery as the JavaScript library
# Turbolinks makes following links in your web application faster. Read more: https://github.com/rails/turbolinks
# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
# bundle exec rake doc:rails generates the API under doc/api.
# gem 'sdoc', '~> 0.4.0',          group: :doc

# Spring speeds up development by keeping your application running in the background. Read more: https://github.com/rails/spring

# Use ActiveModel has_secure_password
# gem 'bcrypt', '~> 3.1.7'

# Use unicorn as the app server
# gem 'unicorn'

# Use Capistrano for deployment
# gem 'capistrano-rails', group: :development

# Use debugger
# gem 'debugger', group: [:development, :test]

# foundation-rails – front-end framework
# gibbon – access to the MailChimp API
# google_drive – use Google Drive spreadsheets for data storage
# high_voltage – for static pages like “about”
# simple_form – forms made easy
# We’ll also add utilities that make development easier:

# better_errors – helps when things go wrong
# quiet_assets – suppresses distracting messages in the log
# rails_layout – generates files for an application layout

# Heroku gems for prod: 
#  pg - PostgreSql - required by heroku, they don't do sqllite
#  thin - web server to use at heroku for now
# rails_12factor - required hanlder for loging and serving css and javascript assets

